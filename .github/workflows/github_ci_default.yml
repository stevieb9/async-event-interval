name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest ]
        perl: [ '5.42', '5.40', '5.34', '5.32', '5.24', '5.20', '5.18', '5.14', '5.12', '5.10' ]
        include:
          - perl: '5.32'
            os: ubuntu-latest
            coverage: true
    name: Perl ${{ matrix.perl }} on ${{ matrix.os }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v2
      - name: Set up perl
        uses: shogo82148/actions-setup-perl@v1
        with:
          perl-version: ${{ matrix.perl }}
      - run: perl -V
      - run: cpanm ExtUtils::PL2Bat
      - run: cpanm ExtUtils::MakeMaker
      - run: |
          perl -e 'exit($] < 5.014 ? 0 : 1)' && cpanm -n https://backpan.perl.org/authors/id/Y/YA/YANICK/Parallel-ForkManager-2.02.tar.gz || true
      - run: cpanm --installdeps .
      - name: Run tests (no coverage)
        if: ${{ !matrix.coverage }}
        run: prove -lv t
      - name: Run tests (with coverage)
        if: ${{ matrix.coverage }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cpanm -n Devel::Cover::Report::Coveralls
          cover -test -report Coveralls
